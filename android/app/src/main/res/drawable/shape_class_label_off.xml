<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners
        android:radius="14dp"
        />
    <stroke
        android:width="0.5dp"
        android:color="@color/color_search_hint"
        />
    <!-- 设置矩形内的颜色,此处为灰色 -->
    <solid android:color="@color/common_bac"/>
</shape>